---
import { formatInTimeZone } from 'date-fns-tz'

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {date}
  {
    // formatInTimeZone(date, 'America/New York', 'MMM do')
  }
</time>
